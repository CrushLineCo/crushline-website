version: 1
frontend:
  phases:
    preBuild:
      commands:
        - if command -v nvm >/dev/null 2>&1; then nvm install; nvm use; fi
        - node -v
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
